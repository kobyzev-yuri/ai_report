# Развертывание RAG системы для KB_billing

## Обзор

RAG система интегрирована в веб-интерфейс Streamlit и доступна в первой закладке "🤖 Ассистент".

## Компоненты

1. **Qdrant** - векторная БД для хранения KB
2. **Sentence Transformers** - модель эмбеддингов для русского языка
3. **RAG модули** - загрузка KB и поиск информации
4. **Streamlit интеграция** - веб-интерфейс ассистента

## Требования

- Python 3.8+
- Docker (для Qdrant)
- Oracle Database (для выполнения SQL запросов)

## Установка

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Запуск Qdrant

```bash
# Запуск Qdrant в Docker
docker run -d \
  --name qdrant \
  -p 6333:6333 \
  -v $(pwd)/qdrant_storage:/qdrant/storage \
  qdrant/qdrant

# Проверка
curl http://localhost:6333/health
```

### 3. Инициализация KB

```bash
# Загрузка всех данных KB в Qdrant
python kb_billing/rag/init_kb.py

# С пересозданием коллекции (если нужно)
python kb_billing/rag/init_kb.py --recreate
```

### 4. Запуск веб-интерфейса

```bash
# Запуск Streamlit
./run_streamlit_background.sh
```

## Конфигурация

### Переменные окружения

Для Qdrant (опционально, по умолчанию localhost:6333):
```bash
export QDRANT_HOST=localhost
export QDRANT_PORT=6333
```

Для Oracle (уже настроены в config.env):
```bash
export ORACLE_USER=...
export ORACLE_PASSWORD=...
export ORACLE_HOST=...
export ORACLE_PORT=1521
export ORACLE_SERVICE=...
```

### Streamlit secrets (опционально)

Создайте `.streamlit/secrets.toml`:
```toml
QDRANT_HOST = "localhost"
QDRANT_PORT = 6333
```

## Использование

### В веб-интерфейсе

1. Откройте веб-интерфейс Streamlit
2. Перейдите на закладку "🤖 Ассистент"
3. Введите вопрос на русском языке
4. Нажмите "🔍 Найти похожие примеры" или "📊 Сгенерировать SQL"

### В Python коде

```python
from kb_billing.rag.rag_assistant import RAGAssistant

assistant = RAGAssistant()

# Поиск похожих примеров
examples = assistant.search_similar_examples(
    "Покажи превышение трафика за октябрь 2025",
    limit=5
)

# Получение контекста для генерации SQL
context = assistant.get_context_for_sql_generation(
    "Покажи себестоимость по всем SUB-"
)
```

## Обновление KB

При добавлении новых примеров или изменении документации:

```bash
# Перезагрузить KB
python kb_billing/rag/init_kb.py --recreate
```

## Мониторинг

### Проверка Qdrant

```bash
# Статус коллекции
curl http://localhost:6333/collections/kb_billing

# Количество точек
curl http://localhost:6333/collections/kb_billing | jq .result.points_count
```

### Логи

Логи RAG системы выводятся в консоль при запуске Streamlit.

## Troubleshooting

### Qdrant не запущен

```bash
# Проверка
docker ps | grep qdrant

# Запуск
docker start qdrant
# или
docker run -d -p 6333:6333 qdrant/qdrant
```

### Коллекция не найдена

```bash
# Пересоздать коллекцию
python kb_billing/rag/init_kb.py --recreate
```

### Ошибки импорта

```bash
# Установить зависимости
pip install qdrant-client sentence-transformers torch transformers
```

### Медленная загрузка модели

При первом запуске модель загружается из HuggingFace. Это может занять несколько минут. Модель кэшируется локально.

## Производительность

- **Загрузка KB**: ~1-2 минуты (зависит от модели)
- **Поиск примеров**: <100ms
- **Генерация контекста**: <200ms

## Безопасность

- Qdrant доступен только локально (localhost:6333)
- Oracle подключение использует существующие credentials из config.env
- SQL запросы выполняются с правами пользователя Oracle

## Масштабирование

Для production окружения:

1. Используйте Qdrant Cloud или отдельный сервер
2. Настройте репликацию Qdrant
3. Используйте более мощную модель эмбеддингов (multilingual-e5-large)
4. Добавьте кэширование результатов поиска

## Поддержка

- Документация: `kb_billing/rag/README.md`
- Быстрый старт: `kb_billing/rag/QUICK_START.md`
- Архитектура: `kb_billing/RAG_ARCHITECTURE_RECOMMENDATION.md`


