# Рекомендации по выбору векторной БД для kb_billing

## Анализ задачи

### Типы данных в kb_billing:
1. **DDL описания** - структурированные SQL схемы (CREATE TABLE, CREATE VIEW)
2. **Документация** - текстовые описания таблиц, колонок, бизнес-правил
3. **Q/A примеры** - пары вопрос (русский) → SQL запрос
4. **Метаданные** - связи между таблицами, домены, категории

### Задачи агента:
1. **Генерация SQL** - построение финансовых отчетов по запросу финансиста
2. **Поиск соответствий** - сверка услуг между системами (Иридиум ↔ Стэкком)
3. **Семантический поиск** - поиск похожих запросов, примеров

### Критерии выбора:
- ✅ **Точность** - критично для SQL генерации (неправильный SQL = ошибка в отчете)
- ✅ **Структурированные данные** - работа с DDL, метаданными
- ✅ **Русский язык** - все вопросы и описания на русском
- ✅ **Гибридный поиск** - комбинация семантического + точного поиска
- ✅ **Простота интеграции** - с существующим стеком (Python, Oracle)

---

## Рекомендации (по приоритету)

### 🥇 1. **Qdrant** (Рекомендуется)

**Почему:**
- ✅ Отличная поддержка русского языка (встроенные эмбеддинги)
- ✅ Гибридный поиск (векторный + фильтры по метаданным)
- ✅ Payload фильтрация - идеально для структурированных данных
- ✅ Простая интеграция с Python
- ✅ Хорошая производительность на небольших объемах
- ✅ Поддержка метаданных (category, complexity, business_entity)

**Архитектура:**
```
Qdrant Collection: kb_billing
├── Vectors (embeddings) - для семантического поиска
├── Payload:
│   ├── type: "ddl" | "documentation" | "qa_example" | "metadata"
│   ├── table_name: "SPNET_TRAFFIC"
│   ├── category: "Сервисы" | "Превышение трафика" | ...
│   ├── complexity: 1-3
│   ├── business_entity: "services" | "overage" | ...
│   └── content: полный текст
└── Filters - для точного поиска по метаданным
```

**Пример использования:**
```python
from qdrant_client import QdrantClient
from qdrant_client.models import Distance, VectorParams, PointStruct

client = QdrantClient("localhost", port=6333)

# Гибридный поиск: семантический + фильтры
results = client.search(
    collection_name="kb_billing",
    query_vector=embedding,
    query_filter={
        "must": [
            {"key": "type", "match": {"value": "qa_example"}},
            {"key": "category", "match": {"value": "Превышение трафика"}}
        ]
    },
    limit=5
)
```

**Плюсы:**
- Лучший баланс точности и гибкости
- Отличная документация
- Активное сообщество

**Минусы:**
- Требует отдельный сервис (но легковесный)

---

### 🥈 2. **pgvector (PostgreSQL)** 

**Почему:**
- ✅ Интеграция с существующей инфраструктурой
- ✅ SQL-интерфейс (знакомый для команды)
- ✅ Гибридный поиск через SQL (векторный + WHERE фильтры)
- ✅ Транзакции и ACID
- ✅ Можно хранить рядом с Oracle (или использовать отдельный PostgreSQL)

**Архитектура:**
```sql
CREATE TABLE kb_billing_vectors (
    id SERIAL PRIMARY KEY,
    content TEXT,
    embedding vector(1536),  -- для OpenAI или 384 для sentence-transformers
    type VARCHAR(50),  -- 'ddl', 'documentation', 'qa_example', 'metadata'
    table_name VARCHAR(100),
    category VARCHAR(100),
    complexity INTEGER,
    business_entity VARCHAR(100),
    metadata JSONB
);

CREATE INDEX ON kb_billing_vectors 
USING ivfflat (embedding vector_cosine_ops);
```

**Пример использования:**
```python
import psycopg2
from pgvector.psycopg2 import register_vector

# Гибридный поиск
query = """
SELECT content, sql_query, 
       1 - (embedding <=> %s::vector) as similarity
FROM kb_billing_vectors
WHERE type = 'qa_example'
  AND category = 'Превышение трафика'
ORDER BY embedding <=> %s::vector
LIMIT 5;
"""
```

**Плюсы:**
- Знакомый стек (SQL)
- Надежность и транзакции
- Можно использовать существующий PostgreSQL

**Минусы:**
- Менее оптимизирован для векторного поиска чем специализированные решения
- Требует настройку индексов

---

### 🥉 3. **Chroma**

**Почему:**
- ✅ Очень простая интеграция (Python-first)
- ✅ Встроенная поддержка метаданных
- ✅ Легковесный (можно встроить в приложение)
- ✅ Хорошо работает с небольшими объемами

**Архитектура:**
```python
import chromadb

client = chromadb.Client()
collection = client.create_collection(
    name="kb_billing",
    metadata={"description": "Billing knowledge base"}
)

# Добавление с метаданными
collection.add(
    documents=[content],
    embeddings=[embedding],
    metadatas=[{
        "type": "qa_example",
        "category": "Превышение трафика",
        "complexity": 2
    }],
    ids=[id]
)

# Поиск с фильтрами
results = collection.query(
    query_embeddings=[query_embedding],
    n_results=5,
    where={"type": "qa_example", "category": "Превышение трафика"}
)
```

**Плюсы:**
- Максимально простая интеграция
- Не требует отдельного сервиса (можно embedded)
- Хорошо для прототипирования

**Минусы:**
- Менее производительный на больших объемах
- Меньше возможностей для тонкой настройки

---

### 4. **Weaviate**

**Почему:**
- ✅ Отличная поддержка метаданных и фильтров
- ✅ GraphQL API
- ✅ Встроенные модули для эмбеддингов
- ✅ Хорошая производительность

**Минусы:**
- Более сложная настройка
- Избыточен для текущей задачи

---

## Рекомендация: **Qdrant**

### Почему именно Qdrant:

1. **Точность поиска** - критично для SQL генерации
   - Гибридный поиск (векторный + фильтры)
   - Payload фильтрация по метаданным

2. **Работа с русским языком**
   - Поддержка мультиязычных эмбеддингов
   - Можно использовать `intfloat/multilingual-e5-large` или `cointegrated/rubert-tiny2`

3. **Структурированные данные**
   - Payload для метаданных (type, category, complexity)
   - Фильтры для точного поиска

4. **Простота интеграции**
   - Python клиент
   - REST API
   - Легко развернуть (Docker)

### Архитектура решения:

```
┌─────────────────────────────────────────┐
│         Финансист (вопрос)              │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│      AI Agent (LLM + RAG)               │
│  ┌──────────────────────────────────┐   │
│  │  1. Понимание запроса            │   │
│  │  2. Поиск в Qdrant (гибридный)   │   │
│  │     - Семантический поиск        │   │
│  │     - Фильтры по метаданным      │   │
│  │  3. Генерация SQL                │   │
│  └──────────────────────────────────┘   │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│         Qdrant (векторная БД)            │
│  ┌──────────────────────────────────┐   │
│  │  Collection: kb_billing          │   │
│  │  ├── DDL (структура БД)          │   │
│  │  ├── Documentation (описания)    │   │
│  │  ├── Q/A Examples (примеры SQL)  │   │
│  │  └── Metadata (связи, правила)   │   │
│  └──────────────────────────────────┘   │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│      Oracle Database (выполнение SQL)   │
└─────────────────────────────────────────┘
```

### Структура данных в Qdrant:

```python
# Пример точки в Qdrant
{
    "id": "qa_example_001",
    "vector": [0.123, 0.456, ...],  # embedding
    "payload": {
        "type": "qa_example",
        "question": "Покажи превышение трафика за октябрь 2025",
        "sql": "SELECT ... FROM v_consolidated_report_with_billing ...",
        "category": "Превышение трафика",
        "complexity": 2,
        "business_entity": "overage",
        "table_names": ["V_CONSOLIDATED_REPORT_WITH_BILLING"],
        "context": "Анализ превышения трафика за период"
    }
}
```

### Стратегия поиска:

1. **Для SQL генерации:**
   - Семантический поиск по вопросу
   - Фильтр: `type = "qa_example"` + `category` (если известна)
   - Возврат топ-5 похожих примеров

2. **Для поиска соответствий (Иридиум ↔ Стэкком):**
   - Семантический поиск по описанию услуги
   - Фильтр: `type = "documentation"` + `table_name IN ["SERVICES", "SPNET_TRAFFIC"]`
   - Поиск связей через `relationships` в payload

3. **Для понимания структуры БД:**
   - Фильтр: `type = "ddl"` + `table_name = "SPNET_TRAFFIC"`
   - Возврат DDL и документации

---

## Эмбеддинги для русского языка

### Рекомендуемые модели:

1. **`intfloat/multilingual-e5-large`** (рекомендуется)
   - Поддержка русского языка
   - Размерность: 1024
   - Хорошее качество

2. **`cointegrated/rubert-tiny2`**
   - Легковесная модель
   - Размерность: 312
   - Быстрая инференция

3. **`sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`**
   - Баланс качества и скорости
   - Размерность: 384

### Пример загрузки:

```python
from sentence_transformers import SentenceTransformer

# Загрузка модели
model = SentenceTransformer('intfloat/multilingual-e5-large')

# Эмбеддинг для русского текста
text = "Покажи превышение трафика за октябрь 2025"
embedding = model.encode(text, normalize_embeddings=True)
```

---

## План внедрения

### Этап 1: Подготовка данных
1. Конвертация JSON → векторы
2. Создание эмбеддингов для всех типов данных
3. Загрузка в Qdrant с метаданными

### Этап 2: Настройка поиска
1. Тестирование гибридного поиска
2. Настройка фильтров
3. Оптимизация количества результатов (top-k)

### Этап 3: Интеграция с агентом
1. RAG pipeline (Retrieval → Augmentation → Generation)
2. Обработка результатов поиска
3. Генерация SQL с контекстом

### Этап 4: Тестирование
1. Тесты на реальных запросах финансистов
2. Проверка точности SQL
3. Оптимизация на основе метрик

---

## Альтернатива: pgvector (если уже есть PostgreSQL)

Если у вас уже есть PostgreSQL сервер, можно использовать pgvector:

**Плюсы:**
- Не нужен отдельный сервис
- Знакомый SQL интерфейс
- Транзакции и надежность

**Минусы:**
- Менее оптимизирован для векторного поиска
- Требует настройку индексов

---

## Итоговая рекомендация

**Выбор: Qdrant**

**Причины:**
1. Лучший баланс точности и гибкости
2. Отличная поддержка русского языка
3. Гибридный поиск (критично для SQL генерации)
4. Простая интеграция
5. Хорошая производительность

**Следующие шаги:**
1. Установить Qdrant (Docker)
2. Выбрать модель эмбеддингов для русского языка
3. Создать скрипт загрузки kb_billing в Qdrant
4. Протестировать поиск на реальных запросах






