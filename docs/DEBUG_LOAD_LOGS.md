# Отладка загрузки CSV/XLSX из Иридиума

## Проблема

При загрузке CSV/XLSX файлов из Иридиума система должна избегать повторной загрузки уже загруженных файлов. Для этого используется таблица `load_logs`, которая ведет учет успешно загруженных файлов. Однако, если в `load_logs` нет записей о ранее загруженных файлах, система будет пытаться загрузить их заново.

## Новая функциональность: Контроль количества записей

Система теперь автоматически:
- **Подсчитывает записи в файле** перед загрузкой (CSV и XLSX)
- **Проверяет количество записей в базе** после загрузки
- **Обнаруживает неполные загрузки** (когда в базе меньше записей, чем в файле)
- **Автоматически перезагружает** файлы с неполными данными

В интерфейсе Streamlit отображаются колонки:
- **Records in File** - количество записей в CSV/XLSX файле
- **Records in DB** - количество записей в базе для этого файла

Если количество записей в базе меньше, чем в файле, система покажет предупреждение и автоматически перезагрузит файл при следующем импорте.

## Решение

### 1. Проверка отсутствующих записей

**Через веб-интерфейс:**
1. Откройте `streamlit_data_loader.py`
2. Перейдите на вкладку **"🔧 Debug Tools"**
3. Нажмите кнопку **"🔍 Проверить отсутствующие записи"**
4. Система покажет все файлы, которые загружены в таблицы, но отсутствуют в `load_logs`

**Через командную строку:**
```bash
cd python
# Для PostgreSQL
python restore_load_history.py --check-only --db-type postgresql

# Для Oracle
python restore_load_history.py --check-only --db-type oracle
```

### 2. Восстановление истории загрузок

**Через веб-интерфейс:**
1. Откройте вкладку **"🔧 Debug Tools"**
2. Нажмите кнопку **"🔄 Восстановить историю загрузок"**
3. Система автоматически создаст записи в `load_logs` для всех файлов, которые уже загружены в таблицы

**Через командную строку:**
```bash
# Для PostgreSQL:
# Сначала проверьте, что будет сделано (dry-run)
python restore_load_history.py --dry-run --db-type postgresql

# Затем выполните восстановление
python restore_load_history.py --db-type postgresql

# Для Oracle:
# Сначала проверьте, что будет сделано (dry-run)
python restore_load_history.py --dry-run --db-type oracle

# Затем выполните восстановление
python restore_load_history.py --db-type oracle
```

### 3. Просмотр статистики

На вкладке **"🔧 Debug Tools"** внизу отображается статистика по `load_logs`:
- Количество записей по таблицам
- Статусы загрузок (SUCCESS, FAILED, PARTIAL)
- Общее количество загруженных записей

## Как это работает

### Скрипт `restore_load_history.py`

Скрипт анализирует данные в таблицах `spnet_traffic` и `steccom_expenses` и:
1. Находит все уникальные `source_file` в каждой таблице
2. Подсчитывает количество записей для каждого файла
3. Определяет даты первой и последней загрузки
4. Проверяет, есть ли уже запись в `load_logs` для этого файла
5. Если записи нет, создает новую запись со статусом `SUCCESS`

### Проверка перед загрузкой

Система загрузки использует метод `is_file_loaded()` который проверяет наличие записи в `load_logs`:

```python
def is_file_loaded(self, file_name, table_name='spnet_traffic'):
    """Проверка, загружен ли файл уже"""
    cursor.execute("""
        SELECT COUNT(*) FROM load_logs 
        WHERE LOWER(file_name) = LOWER(%s) 
        AND LOWER(table_name) = LOWER(%s)
        AND load_status = 'SUCCESS'
    """, (file_name, table_name))
    return cursor.fetchone()[0] > 0
```

## Рекомендации для отладки

1. **Перед первой загрузкой:** Запустите проверку отсутствующих записей, чтобы увидеть текущее состояние
2. **После восстановления:** Проверьте статистику в `load_logs`, чтобы убедиться, что записи созданы
3. **При проблемах:** Используйте вкладку **"📋 Load History"** для просмотра всех записей о загрузках
4. **Для Oracle:** Используйте `--db-type oracle` в командной строке. Скрипт автоматически использует правильные имена столбцов (FILE_NAME вместо source_file, LOADED_BY вместо created_by)

## Структура load_logs

Таблица `load_logs` содержит следующие поля:
- `id` - уникальный идентификатор
- `table_name` - имя таблицы (spnet_traffic, steccom_expenses)
- `file_name` - имя загруженного файла
- `records_loaded` - количество загруженных записей
- `load_status` - статус (SUCCESS, FAILED, PARTIAL)
- `error_message` - сообщение об ошибке (если есть)
- `load_start_time` - время начала загрузки
- `load_end_time` - время окончания загрузки
- `loaded_by` - кто выполнил загрузку

## Примеры использования

### Проверка только SPNet файлов
```sql
SELECT 
    t.source_file,
    COUNT(*) as records_count
FROM spnet_traffic t
WHERE t.source_file IS NOT NULL
AND NOT EXISTS (
    SELECT 1 FROM load_logs l
    WHERE LOWER(l.file_name) = LOWER(t.source_file)
    AND LOWER(l.table_name) = 'spnet_traffic'
    AND l.load_status = 'SUCCESS'
)
GROUP BY t.source_file;
```

### Просмотр всех успешных загрузок
```sql
SELECT 
    table_name,
    file_name,
    records_loaded,
    load_start_time
FROM load_logs
WHERE load_status = 'SUCCESS'
ORDER BY load_start_time DESC;
```

