# 🚀 Краткая сводка: Методы развертывания ai_report

## 📊 Обзор проекта

**ai_report** - RAG-система для финансовых отчетов Iridium M2M
- 🤖 AI-ассистент (GPT-4o) для генерации SQL
- 🗄️ Векторная БД Qdrant для базы знаний
- 🌐 Веб-интерфейс Streamlit
- 💾 Oracle Database

**Сервер:** 82.114.2.2:1194  
**Директория:** `/usr/local/projects/ai_report`

---

## 🎯 Быстрый старт (3 команды)

```bash
cd deploy

# 1. Проверка готовности
./check_prerequisites.sh

# 2. Обследование сервера
SSH_CMD="ssh -p 1194" ./server_inspection.sh root@82.114.2.2

# 3. Развертывание
SSH_CMD="ssh -p 1194" ./safe_deploy.sh root@82.114.2.2
```

---

## 📋 Методы развертывания

### 1️⃣ Автоматическое безопасное развертывание ⭐ РЕКОМЕНДУЕТСЯ

```bash
SSH_CMD="ssh -p 1194" ./safe_deploy.sh root@82.114.2.2
```

**Что делает:**
- ✅ Обследование сервера
- ✅ Синхронизация файлов
- ✅ Остановка старого Streamlit
- ✅ Проверка конфигурации
- ✅ Развертывание контейнеров
- ✅ Проверка работоспособности

**Когда использовать:** Первое развертывание, полное обновление

---

### 2️⃣ Docker Compose развертывание

```bash
cd deploy
cp config.env.example config.env
nano config.env  # Настроить Oracle

./deploy.sh docker
```

**Что включает:**
- 🐳 Qdrant (порт 6333)
- 🌐 Streamlit (порт 8504)
- 🔄 Автоматическая инициализация KB

**Когда использовать:** Production окружение

---

### 3️⃣ Синхронизация файлов (rsync)

```bash
SSH_CMD="ssh -p 1194" ./sync_deploy.sh root@82.114.2.2
```

**Что синхронизируется:**
- Python приложение
- RAG модули
- База знаний (KB)
- Oracle скрипты
- Docker файлы

**Когда использовать:** Быстрое обновление кода

---

### 4️⃣ Обновление базы знаний (KB)

**Вариант A: Обновление (добавление новых данных)**
```bash
SSH_CMD="ssh -p 1194" ./sync_and_update_kb.sh root@82.114.2.2
```

**Вариант B: Полная перестройка (удаление старых данных)**
```bash
SSH_CMD="ssh -p 1194" ./sync_and_rebuild_kb.sh root@82.114.2.2
```

**Когда использовать:** Обновление примеров SQL, описаний таблиц/VIEW

---

### 5️⃣ Ручное развертывание

```bash
# 1. Установка зависимостей
pip install -r requirements.txt

# 2. Запуск Qdrant
docker run -d --name ai_report_qdrant -p 6333:6333 qdrant/qdrant:latest

# 3. Инициализация KB
./init_kb.sh

# 4. Запуск Streamlit
./run_streamlit_background.sh
```

**Когда использовать:** Отладка, детальный контроль

---

## 🔄 Типичные сценарии

### Сценарий 1: Обновление кода Streamlit

```bash
# 1. Синхронизация
cd deploy
SSH_CMD="ssh -p 1194" ./sync_deploy.sh root@82.114.2.2

# 2. Перезапуск
ssh -p 1194 root@82.114.2.2 "cd /usr/local/projects/ai_report && docker-compose restart streamlit"
```

---

### Сценарий 2: Обновление Oracle VIEW

```bash
# 1. Синхронизация
cd deploy
SSH_CMD="ssh -p 1194" ./sync_deploy.sh root@82.114.2.2

# 2. Применение VIEW
ssh -p 1194 root@82.114.2.2 "source /usr/local/projects/ai_report/config.env && \
  cd /usr/local/projects/ai_report/oracle/views && \
  sqlplus \${ORACLE_USER}/\${ORACLE_PASSWORD}@\${ORACLE_HOST}:\${ORACLE_PORT}/\${ORACLE_SERVICE} \
  @04_v_consolidated_report_with_billing.sql"
```

---

### Сценарий 3: Добавление новых примеров SQL в KB

```bash
# 1. Редактирование локально
vim kb_billing/training_data/sql_examples.json

# 2. Синхронизация + обновление KB
SSH_CMD="ssh -p 1194" ./sync_and_update_kb.sh root@82.114.2.2
```

---

### Сценарий 4: Полная перестройка системы

```bash
# 1. Резервное копирование
ssh -p 1194 root@82.114.2.2 "tar czf /tmp/ai_report_backup_$(date +%Y%m%d_%H%M%S).tar.gz /usr/local/projects/ai_report"

# 2. Безопасное развертывание
cd deploy
SSH_CMD="ssh -p 1194" ./safe_deploy.sh root@82.114.2.2
```

---

## 🛠️ Вспомогательные команды

### Проверка статуса
```bash
ssh -p 1194 root@82.114.2.2 "cd /usr/local/projects/ai_report && ./status_all.sh"
```

### Просмотр логов
```bash
ssh -p 1194 root@82.114.2.2 "cd /usr/local/projects/ai_report && docker-compose logs -f streamlit"
```

### Проверка здоровья
```bash
# Qdrant
ssh -p 1194 root@82.114.2.2 "curl http://localhost:6333/health"

# Streamlit
ssh -p 1194 root@82.114.2.2 "curl http://localhost:8504/_stcore/health"

# KB коллекция
ssh -p 1194 root@82.114.2.2 "curl http://localhost:6333/collections/kb_billing"
```

### Перезапуск сервисов
```bash
# Streamlit
ssh -p 1194 root@82.114.2.2 "cd /usr/local/projects/ai_report && docker-compose restart streamlit"

# Qdrant
ssh -p 1194 root@82.114.2.2 "docker restart ai_report_qdrant"

# Все сервисы
ssh -p 1194 root@82.114.2.2 "cd /usr/local/projects/ai_report && docker-compose restart"
```

---

## 📊 Сравнение методов

| Метод | Время | Сложность | Безопасность | Использование |
|-------|-------|-----------|--------------|---------------|
| safe_deploy.sh | 5 мин | ⭐⭐ | ⭐⭐⭐⭐⭐ | Первое развертывание |
| Docker Compose | 3 мин | ⭐⭐⭐ | ⭐⭐⭐⭐ | Production |
| rsync | 1 мин | ⭐⭐ | ⭐⭐⭐⭐ | Обновления кода |
| KB Update | 2 мин | ⭐ | ⭐⭐⭐⭐⭐ | Обновление примеров |
| Ручное | 10 мин | ⭐⭐⭐⭐ | ⭐⭐⭐ | Отладка |

---

## 🔐 Безопасность

### Перед развертыванием:

1. **Резервное копирование:**
   ```bash
   ssh -p 1194 root@82.114.2.2 "tar czf /tmp/ai_report_backup_$(date +%Y%m%d_%H%M%S).tar.gz /usr/local/projects/ai_report"
   ```

2. **Проверка config.env:**
   ```bash
   ssh -p 1194 root@82.114.2.2 "cat /usr/local/projects/ai_report/config.env | grep -E '^ORACLE_|^QDRANT_'"
   ```

3. **Защита паролей:**
   ```bash
   chmod 600 config.env
   ```

---

## 🐛 Troubleshooting

### Порт 8504 занят
```bash
ssh -p 1194 root@82.114.2.2 "lsof -i :8504"
ssh -p 1194 root@82.114.2.2 "kill -9 <PID>"
```

### Qdrant не запускается
```bash
ssh -p 1194 root@82.114.2.2 "docker logs ai_report_qdrant"
ssh -p 1194 root@82.114.2.2 "docker restart ai_report_qdrant"
```

### KB не инициализирована
```bash
ssh -p 1194 root@82.114.2.2 "cd /usr/local/projects/ai_report && ./init_kb.sh --recreate"
```

### Streamlit не запускается
```bash
ssh -p 1194 root@82.114.2.2 "cd /usr/local/projects/ai_report && docker-compose logs streamlit"
ssh -p 1194 root@82.114.2.2 "cd /usr/local/projects/ai_report && docker-compose restart streamlit"
```

---

## 📁 Ключевые файлы

```
ai_report/
├── deploy/
│   ├── safe_deploy.sh              ⭐ Безопасное развертывание
│   ├── deploy.sh                   Docker/ручное развертывание
│   ├── sync_deploy.sh              Синхронизация файлов
│   ├── server_inspection.sh        Обследование сервера
│   ├── init_kb.sh                  Инициализация KB
│   └── docker-compose.yml          Docker Compose конфигурация
│
├── sync_and_update_kb.sh           Синхронизация + обновление KB
├── sync_and_rebuild_kb.sh          Синхронизация + перестройка KB
│
├── run_streamlit_background.sh     Запуск Streamlit
├── stop_streamlit.sh               Остановка Streamlit
├── restart_streamlit.sh            Перезапуск Streamlit
│
└── cursor.md                       ⭐ Инструкция для Cursor IDE
```

---

## 🌐 Доступ к приложению

**Прямой доступ:** http://82.114.2.2:8504  
**Через Nginx:** http://stat.steccom.ru:7776/ai_report

**Основные вкладки:**
- 🤖 **Ассистент** - RAG-ассистент для SQL
- 📈 **Финансовый анализ** - анализ прибыльности
- **Главный отчет** - сводная таблица
- **Data Loader** - загрузка данных

---

## ✅ Чеклист после развертывания

- [ ] Qdrant доступен (порт 6333)
- [ ] Streamlit доступен (порт 8504)
- [ ] KB инициализирована
- [ ] Ассистент работает
- [ ] Поиск примеров работает
- [ ] SQL запросы выполняются
- [ ] Логи без ошибок
- [ ] Nginx proxy работает

---

## 📖 Документация

**Основная:**
- `README.md` - Описание проекта
- `cursor.md` - Работа в Cursor IDE
- `DEPLOYMENT_METHODS_ANALYSIS.md` - Детальный анализ методов

**В deploy/:**
- `START_HERE.md` - Начните отсюда
- `DEPLOYMENT_SERVER.md` - Развертывание на сервере
- `DEPLOYMENT_RAG.md` - RAG система
- `QUICK_DEPLOY.md` - Быстрый старт

---

## 💡 Рекомендации

1. **Для первого развертывания:** используйте `safe_deploy.sh`
2. **Для обновлений кода:** используйте `sync_deploy.sh` + перезапуск
3. **Для обновления KB:** используйте `sync_and_update_kb.sh`
4. **Для отладки:** используйте ручное развертывание
5. **Всегда делайте резервные копии** перед развертыванием

---

**Проект:** Iridium M2M Reporting  
**Технологии:** Oracle, Python, Streamlit, Qdrant, Docker  
**Дата:** Октябрь 2025
