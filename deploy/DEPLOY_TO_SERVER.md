# Развертывание на сервере 82.114.2.2

## Быстрый старт

### Вариант 1: Автоматическое развертывание (рекомендуется)

```bash
cd deploy
./safe_deploy.sh root@82.114.2.2
```

Скрипт выполнит все шаги автоматически:
1. ✅ Обследование сервера
2. ✅ Синхронизация файлов
3. ✅ Остановка старого Streamlit
4. ✅ Развертывание контейнеров
5. ✅ Проверка развертывания

---

### Вариант 2: Пошаговое развертывание

#### Шаг 1: Обследование

```bash
cd deploy
./server_inspection.sh root@82.114.2.2
```

**Проверяет:**
- Docker и Docker Compose
- Текущее состояние сервера
- Запущенные процессы и порты
- Конфигурацию

#### Шаг 2: Синхронизация

```bash
./sync_deploy.sh root@82.114.2.2
```

**Синхронизирует:**
- Все файлы deploy директории
- RAG модули
- KB данные
- Docker файлы

#### Шаг 3: Развертывание на сервере

```bash
ssh root@82.114.2.2
cd /usr/local/projects/ai_report

# Остановка старого Streamlit
if [ -f streamlit_8504.pid ]; then
    kill $(cat streamlit_8504.pid)
    rm streamlit_8504.pid
fi

# Развертывание
./deploy.sh docker
```

---

## Что будет проверено

### Обследование сервера:

1. ✅ **Docker** - версия и статус
2. ✅ **Docker Compose** - наличие и версия
3. ✅ **Директория** - `/usr/local/projects/ai_report`
4. ✅ **Контейнеры** - запущенные Qdrant/Streamlit
5. ✅ **Порты** - 8504 (Streamlit), 6333 (Qdrant)
6. ✅ **Процессы** - запущенный Streamlit
7. ✅ **Диск** - свободное место
8. ✅ **Конфигурация** - config.env

### Безопасность:

- ✅ Проверка перед остановкой старого сервиса
- ✅ Резервное копирование (рекомендуется)
- ✅ Проверка конфликтов портов
- ✅ Валидация конфигурации

---

## Важные моменты

### Перед развертыванием:

1. **Резервное копирование:**
   ```bash
   ssh root@82.114.2.2 "tar czf /tmp/ai_report_backup_$(date +%Y%m%d_%H%M%S).tar.gz /usr/local/projects/ai_report"
   ```

2. **Проверка config.env:**
   ```bash
   ssh root@82.114.2.2 "cat /usr/local/projects/ai_report/config.env | grep -E '^ORACLE_|^QDRANT_'"
   ```

3. **Остановка старого Streamlit:**
   - Скрипт автоматически остановит процесс на порту 8504
   - Проверит занятость порта

### После развертывания:

1. **Проверка статуса:**
   ```bash
   ssh root@82.114.2.2 "cd /usr/local/projects/ai_report && ./status_all.sh"
   ```

2. **Проверка логов:**
   ```bash
   ssh root@82.114.2.2 "cd /usr/local/projects/ai_report && docker-compose logs -f streamlit"
   ```

3. **Проверка в браузере:**
   - Откройте `http://82.114.2.2:8504`
   - Перейдите на закладку "🤖 Ассистент"
   - Проверьте работу поиска

---

## Откат (если что-то пошло не так)

```bash
ssh root@82.114.2.2
cd /usr/local/projects/ai_report

# Остановка новых контейнеров
docker-compose down

# Запуск старого Streamlit (если нужно)
./run_streamlit_background.sh
```

---

## Команды для проверки

```bash
# Статус
ssh root@82.114.2.2 "cd /usr/local/projects/ai_report && ./status_all.sh"

# Логи
ssh root@82.114.2.2 "cd /usr/local/projects/ai_report && docker-compose logs -f"

# Проверка здоровья
ssh root@82.114.2.2 "curl http://localhost:6333/health"
ssh root@82.114.2.2 "curl http://localhost:8504/_stcore/health"
```

---

## Документация

- Полная инструкция: `DEPLOYMENT_SERVER.md`
- Развертывание RAG: `DEPLOYMENT_RAG.md`
- Резюме: `DEPLOYMENT_SUMMARY.md`



