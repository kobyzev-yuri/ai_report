# Руководство по безопасной работе с модифицирующими SQL запросами

## Обзор

Система безопасного выполнения SQL предназначена для работы с ответственными операциями (INSERT, UPDATE, DELETE) в базе данных Oracle. Она включает:

- ✅ Валидацию SQL запросов
- 👁️ Предварительный просмотр изменений (dry-run)
- 🔄 Транзакции с возможностью отката
- 📝 Логирование всех операций
- ⚠️ Предупреждения о потенциально опасных операциях

## Режимы работы

### 1. 🔍 Только предпросмотр (dry-run)

**Рекомендуется для отладки и тестирования**

- Запрос анализируется, но **НЕ выполняется**
- Показывается предварительный просмотр изменений
- Безопасно для экспериментов

**Использование:**
- Тестирование новых запросов
- Проверка логики перед выполнением
- Обучение на примерах

### 2. ✅ Выполнение с подтверждением

**Рекомендуется для продакшн операций**

- Требуется явное подтверждение перед выполнением
- Показывается предварительный просмотр
- Изменения можно откатить до коммита

**Использование:**
- Ответственные операции (обновление тарифов, налогов)
- Массовые изменения данных
- Критичные бизнес-операции

### 3. 🚀 Прямое выполнение

**Только для опытных пользователей!**

- Запрос выполняется сразу без подтверждения
- Используйте только если полностью уверены

## Примеры использования

### Пример 1: Обновление налога в тарифных элементах

**Задача:** Изменить налог с 20% на 22% для тарифных элементов, которые начинают действие с 2026 года.

**SQL запрос:**
```sql
UPDATE TARIFF_EL 
SET MONEY = MONEY * 1.1  -- Увеличение на 10% (20% -> 22% = 1.1x)
WHERE TARIFF_ID IN (
    SELECT TARIFF_ID FROM TARIFF 
    WHERE TARIFF_TYPE = 'ABONENTKA'
)
AND START_DATE >= TO_DATE('2026-01-01', 'YYYY-MM-DD')
AND TAX_RATE = 20
```

**Процесс:**
1. Введите запрос в режиме "🔍 Только предпросмотр"
2. Проверьте количество затронутых строк
3. Просмотрите данные, которые будут изменены
4. Переключитесь на "✅ Выполнение с подтверждением"
5. Подтвердите выполнение
6. Проверьте результат

### Пример 2: Создание новых тарифных элементов на следующий год

**Задача:** Создать копии тарифных элементов на 2026 год с обновленным налогом.

**SQL запрос:**
```sql
INSERT INTO TARIFF_EL (TARIFF_ID, ZONE_ID, MONEY, START_DATE, TAX_RATE)
SELECT 
    TARIFF_ID,
    ZONE_ID,
    MONEY * 1.1,  -- Увеличение на 10%
    TO_DATE('2026-01-01', 'YYYY-MM-DD'),
    22  -- Новый налог 22%
FROM TARIFF_EL
WHERE START_DATE = TO_DATE('2025-01-01', 'YYYY-MM-DD')
AND TAX_RATE = 20
```

**Процесс:**
1. Используйте режим предпросмотра для проверки данных
2. Убедитесь, что SELECT возвращает правильные строки
3. Выполните с подтверждением

## Валидация и безопасность

### Автоматические проверки

Система автоматически проверяет:

- ✅ Синтаксис SQL
- ✅ Наличие WHERE в UPDATE/DELETE (предупреждение)
- ✅ Опасные операции (DROP, TRUNCATE - блокируются)
- ✅ Затронутые таблицы
- ✅ Количество затронутых строк

### Предупреждения

Система предупреждает о:

- ⚠️ UPDATE без WHERE (обновит все строки)
- ⚠️ DELETE без WHERE (удалит все строки)
- ⚠️ Большое количество затронутых строк (>1000)

## Логирование

Все операции логируются в:
```
logs/sql_executions/sql_execution_YYYYMMDD.jsonl
```

Каждая запись содержит:
- Timestamp выполнения
- SQL запрос
- Результат (успех/ошибка)
- Количество затронутых строк
- Время выполнения

## Рекомендации по отладке

### 1. Используйте тестовые таблицы

Создайте копии таблиц для тестирования:

```sql
-- Создание тестовой таблицы
CREATE TABLE TARIFF_EL_TEST AS SELECT * FROM TARIFF_EL WHERE ROWNUM <= 100;

-- Тестирование на тестовой таблице
UPDATE TARIFF_EL_TEST SET MONEY = MONEY * 1.1 WHERE ...;

-- После проверки - применение к реальной таблице
UPDATE TARIFF_EL SET MONEY = MONEY * 1.1 WHERE ...;
```

### 2. Начните с SELECT

Перед UPDATE/DELETE всегда проверяйте данные через SELECT:

```sql
-- Сначала проверьте, что будет изменено
SELECT * FROM TARIFF_EL 
WHERE TARIFF_ID IN (SELECT TARIFF_ID FROM TARIFF WHERE TARIFF_TYPE = 'ABONENTKA')
AND START_DATE >= TO_DATE('2026-01-01', 'YYYY-MM-DD')
AND TAX_RATE = 20;

-- Затем выполните UPDATE
UPDATE TARIFF_EL SET MONEY = MONEY * 1.1 WHERE ...;
```

### 3. Используйте транзакции

Всегда работайте в транзакциях:

```sql
-- Начало транзакции (автоматически)
UPDATE TARIFF_EL SET ...;

-- Проверка результата
SELECT * FROM TARIFF_EL WHERE ...;

-- Если все правильно - COMMIT
-- Если ошибка - ROLLBACK
```

### 4. Сохраняйте примеры в KB

После успешной отладки сохраните запрос в KB через вкладку "📚 Расширение KB":

1. Введите вопрос: "Обновить налог с 20% на 22% для тарифных элементов на 2026 год"
2. Вставьте отлаженный SQL
3. Сохраните пример
4. Переобучите KB

## Работа с тестовой средой

### Вариант 1: Тестовые таблицы в той же БД

```sql
-- Создание тестовых таблиц
CREATE TABLE TARIFF_EL_TEST AS SELECT * FROM TARIFF_EL;
CREATE TABLE TARIFF_TEST AS SELECT * FROM TARIFF;

-- Тестирование на тестовых таблицах
UPDATE TARIFF_EL_TEST SET MONEY = MONEY * 1.1 WHERE ...;

-- После проверки - применение к реальным таблицам
UPDATE TARIFF_EL SET MONEY = MONEY * 1.1 WHERE ...;
```

### Вариант 2: Отдельная тестовая БД

Если есть доступ к тестовой БД:

1. Настройте отдельное подключение в `config.env`:
   ```
   ORACLE_HOST_TEST=test-db-host
   ORACLE_USER_TEST=test_user
   ...
   ```

2. Тестируйте запросы на тестовой БД
3. После проверки применяйте на продакшн

### Вариант 3: Dry-run режим

Используйте режим "🔍 Только предпросмотр" для безопасного тестирования:

- Запрос анализируется, но не выполняется
- Показывается, что будет изменено
- Безопасно для экспериментов

## Чеклист перед выполнением

- [ ] Запрос протестирован в режиме dry-run
- [ ] Проверено количество затронутых строк
- [ ] Просмотрены данные, которые будут изменены
- [ ] WHERE условие корректно и не слишком широкое
- [ ] Есть резервная копия данных (если критично)
- [ ] Запрос сохранен в KB для будущего использования

## Обучающие примеры для KB

После успешной отладки сохраните примеры в KB:

**Пример 1:**
- Вопрос: "Обновить налог с 20% на 22% для тарифных элементов абонентской платы на 2026 год"
- SQL: (отлаженный запрос)
- Категория: "Тарифы"
- Сложность: 3

**Пример 2:**
- Вопрос: "Создать копии тарифных элементов на следующий год с обновленным налогом"
- SQL: (отлаженный запрос)
- Категория: "Тарифы"
- Сложность: 4

## Поддержка

При возникновении проблем:
1. Проверьте логи в `logs/sql_executions/`
2. Используйте режим dry-run для отладки
3. Начните с простых запросов
4. Сохраняйте успешные примеры в KB






