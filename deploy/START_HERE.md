# 🚀 Начните отсюда: Развертывание на сервере 82.114.2.2

## Быстрый старт (3 команды)

### Вариант 1: Автоматическое определение SSH (рекомендуется)

```bash
cd deploy

# 1. Проверка готовности
./check_prerequisites.sh

# 2. Быстрое обследование (автоматически определит SSH команду)
./quick_inspect.sh root@82.114.2.2

# 3. Развертывание (укажите SSH команду)
SSH_CMD="ssh -p 1194" ./safe_deploy.sh root@82.114.2.2
# или
SSH_CMD="vz2" ./safe_deploy.sh root@82.114.2.2
```

### Вариант 2: Ручное указание SSH команды

```bash
cd deploy

# 1. Проверка готовности
./check_prerequisites.sh

# 2. Обследование сервера (с кастомным SSH)
SSH_CMD="ssh -p 1194" ./server_inspection.sh root@82.114.2.2
# или с алиасом:
SSH_CMD="vz2" ./server_inspection.sh root@82.114.2.2

# 3. Развертывание (автоматически использует SSH_CMD)
SSH_CMD="ssh -p 1194" ./safe_deploy.sh root@82.114.2.2
```

**Или экспортируйте переменную один раз:**
```bash
export SSH_CMD="ssh -p 1194"
# или
export SSH_CMD="vz2"

./server_inspection.sh root@82.114.2.2
./safe_deploy.sh root@82.114.2.2
```

---

## 📋 Что делают скрипты

### `check_prerequisites.sh`
Проверяет локальные требования (SSH, rsync, файлы)

### `server_inspection.sh`
**Безопасное обследование сервера** (не изменяет ничего):
- Проверяет Docker и Docker Compose
- Проверяет текущее состояние сервера
- Находит запущенные процессы и контейнеры
- Проверяет занятые порты
- Показывает конфигурацию

### `sync_deploy.sh`
Синхронизирует все файлы на сервер через rsync

### `safe_deploy.sh`
**Полное автоматическое развертывание:**
1. Обследование сервера
2. Синхронизация файлов
3. Остановка старого Streamlit на порту 8504
4. Проверка конфигурации
5. Развертывание контейнеров
6. Проверка развертывания

---

## ⚠️ Важно перед развертыванием

1. **Резервное копирование:**
   ```bash
   ssh root@82.114.2.2 "tar czf /tmp/ai_report_backup_$(date +%Y%m%d_%H%M%S).tar.gz /usr/local/projects/ai_report"
   ```

2. **Проверка config.env на сервере:**
   ```bash
   ssh root@82.114.2.2 "cat /usr/local/projects/ai_report/config.env | grep -E '^ORACLE_|^QDRANT_'"
   ```

---

## 📖 Документация

- **План развертывания:** `DEPLOYMENT_PLAN.md` ⭐
- **Развертывание на сервере:** `DEPLOYMENT_SERVER.md`
- **Развертывание RAG:** `DEPLOYMENT_RAG.md`
- **Быстрый старт:** `QUICK_DEPLOY.md`

---

## ✅ После развертывания

```bash
# Проверка статуса
ssh root@82.114.2.2 "cd /usr/local/projects/ai_report && ./status_all.sh"

# Логи
ssh root@82.114.2.2 "cd /usr/local/projects/ai_report && docker-compose logs -f streamlit"
```

**Веб-интерфейс:** `http://82.114.2.2:8504` → Закладка "🤖 Ассистент"

