# Развертывание на сервере 82.114.2.2

## Обзор

Пошаговое руководство по безопасному развертыванию RAG системы на production сервере.

## Предварительные требования

- SSH доступ к серверу `82.114.2.2`
- Docker и Docker Compose на сервере
- Доступ к Oracle Database (billing schema)

## Процесс развертывания

### Шаг 1: Обследование сервера

**Цель:** Проверить текущее состояние сервера перед развертыванием.

```bash
cd deploy
./server_inspection.sh root@82.114.2.2
```

**Что проверяется:**
- ✅ Подключение к серверу
- ✅ Docker и Docker Compose
- ✅ Текущее состояние директории `/usr/local/projects/ai_report`
- ✅ Запущенные контейнеры
- ✅ Занятые порты (8504, 6333)
- ✅ Запущенные процессы Streamlit
- ✅ Дисковое пространство
- ✅ Конфигурация (config.env)

### Шаг 2: Синхронизация файлов

**Цель:** Синхронизировать deploy директорию на сервер.

```bash
./sync_deploy.sh root@82.114.2.2
```

**Что синхронизируется:**
- ✅ Основные файлы приложения (Streamlit)
- ✅ RAG модули (`kb_billing/rag/`)
- ✅ База знаний (KB данные)
- ✅ Oracle скрипты
- ✅ Python скрипты
- ✅ Docker файлы (docker-compose.yml, Dockerfile)
- ✅ Скрипты управления
- ✅ Документация

### Шаг 3: Безопасное развертывание

**Цель:** Полное развертывание с проверками безопасности.

```bash
./safe_deploy.sh root@82.114.2.2
```

**Что выполняется:**
1. Обследование сервера
2. Синхронизация файлов
3. Остановка старого Streamlit на порту 8504
4. Проверка конфигурации
5. Развертывание контейнеров
6. Проверка развертывания

## Ручное развертывание (пошагово)

Если автоматическое развертывание не подходит, выполните вручную:

### 1. Подключение к серверу

```bash
ssh root@82.114.2.2
cd /usr/local/projects/ai_report
```

### 2. Проверка текущего состояния

```bash
# Проверка запущенного Streamlit
ps aux | grep streamlit
cat streamlit_8504.pid 2>/dev/null || echo "PID файл не найден"

# Проверка портов
netstat -tuln | grep -E '8504|6333'

# Проверка контейнеров
docker ps | grep -E 'qdrant|streamlit'
```

### 3. Остановка старого Streamlit

```bash
# Если запущен через скрипт
if [ -f streamlit_8504.pid ]; then
    PID=$(cat streamlit_8504.pid)
    kill $PID
    rm streamlit_8504.pid
fi

# Если запущен через Docker
docker stop $(docker ps -q --filter "name=streamlit") 2>/dev/null || true

# Проверка порта
lsof -i :8504 || echo "Порт 8504 свободен"
```

### 4. Настройка конфигурации

```bash
# Создание config.env (если не существует)
cp config.env.example config.env
nano config.env  # Заполните Oracle настройки

# Проверка настроек
grep -E '^ORACLE_|^QDRANT_' config.env
```

### 5. Развертывание через Docker Compose

```bash
# Развертывание
./deploy.sh docker

# Или напрямую
docker-compose up -d --build

# Проверка статуса
docker-compose ps
./status_all.sh
```

### 6. Проверка работы

```bash
# Логи
docker-compose logs -f streamlit
docker-compose logs -f qdrant

# Проверка здоровья
curl http://localhost:6333/health
curl http://localhost:8504/_stcore/health

# Проверка KB
curl http://localhost:6333/collections/kb_billing
```

## Откат (Rollback)

Если что-то пошло не так:

```bash
# Остановка новых контейнеров
docker-compose down

# Запуск старого Streamlit (если нужно)
cd /usr/local/projects/ai_report
./run_streamlit_background.sh
```

## Безопасность

### Рекомендации:

1. **Резервное копирование перед развертыванием:**
   ```bash
   ssh root@82.114.2.2 "tar czf /tmp/ai_report_backup_$(date +%Y%m%d_%H%M%S).tar.gz /usr/local/projects/ai_report"
   ```

2. **Проверка config.env:**
   - Убедитесь, что пароли не в открытом виде
   - Используйте переменные окружения где возможно

3. **Ограничение доступа к портам:**
   - Qdrant (6333) должен быть доступен только локально
   - Streamlit (8504) через nginx proxy с SSL

4. **Мониторинг:**
   - Настройте алерты на сбои контейнеров
   - Мониторинг использования ресурсов

## Troubleshooting

### Проблема: Порт 8504 занят

```bash
# Найти процесс
lsof -i :8504
# или
fuser 8504/tcp

# Остановить
kill -9 <PID>
```

### Проблема: Qdrant не запускается

```bash
# Проверка логов
docker logs ai_report_qdrant

# Проверка порта
netstat -tuln | grep 6333

# Перезапуск
docker restart ai_report_qdrant
```

### Проблема: KB не инициализирована

```bash
# Проверка коллекции
curl http://localhost:6333/collections/kb_billing

# Переинициализация
./init_kb.sh --recreate
```

### Проблема: Streamlit не запускается

```bash
# Проверка логов
docker-compose logs streamlit

# Проверка зависимостей
docker-compose exec streamlit pip list | grep -E 'qdrant|sentence-transformers'

# Перезапуск
docker-compose restart streamlit
```

## Проверка после развертывания

### Чеклист:

- [ ] Qdrant доступен на порту 6333
- [ ] Streamlit доступен на порту 8504
- [ ] KB инициализирована (проверить коллекцию)
- [ ] Ассистент работает (открыть закладку "🤖 Ассистент")
- [ ] Поиск примеров работает
- [ ] Выполнение SQL запросов работает
- [ ] Логи не содержат ошибок

### Команды проверки:

```bash
# Статус всех сервисов
./status_all.sh

# Проверка здоровья
curl http://localhost:6333/health
curl http://localhost:8504/_stcore/health

# Проверка KB
curl http://localhost:6333/collections/kb_billing | jq '.result.points_count'
```

## Дополнительная информация

- Полная документация: `DEPLOYMENT_RAG.md`
- Быстрый старт: `QUICK_DEPLOY.md`
- Резюме: `DEPLOYMENT_SUMMARY.md`

