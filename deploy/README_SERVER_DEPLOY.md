# Развертывание на сервере 82.114.2.2

## 🎯 Цель

Безопасное развертывание RAG системы с Qdrant и Streamlit на production сервере.

## 📋 Процесс развертывания

### Этап 1: Подготовка (локально)

```bash
cd deploy

# Проверка предварительных требований
./check_prerequisites.sh
```

**Проверяет:**
- SSH и rsync
- Необходимые файлы
- RAG модули

### Этап 2: Обследование сервера

```bash
./server_inspection.sh root@82.114.2.2
```

**Что проверяется:**
- ✅ Docker и Docker Compose на сервере
- ✅ Текущее состояние `/usr/local/projects/ai_report`
- ✅ Запущенные контейнеры и процессы
- ✅ Занятые порты (8504, 6333)
- ✅ Конфигурация (config.env)
- ✅ Дисковое пространство

**Важно:** Обследование не изменяет ничего на сервере, только читает информацию.

### Этап 3: Синхронизация файлов

```bash
./sync_deploy.sh root@82.114.2.2
```

**Синхронизирует:**
- Все файлы из deploy директории
- RAG модули (`kb_billing/rag/`)
- База знаний (KB данные)
- Docker файлы
- Скрипты управления

**Требования:**
- rsync должен быть установлен локально
- SSH доступ к серверу

### Этап 4: Безопасное развертывание

```bash
./safe_deploy.sh root@82.114.2.2
```

**Выполняет:**
1. Обследование сервера
2. Синхронизацию файлов
3. Остановку старого Streamlit на порту 8504
4. Проверку конфигурации
5. Развертывание через Docker Compose
6. Проверку развертывания

---

## 🔍 Детальное обследование

### Что проверяет `server_inspection.sh`:

1. **Подключение к серверу**
   - SSH доступность
   - Таймаут подключения

2. **Docker**
   - Версия Docker
   - Статус службы
   - Docker Compose (v1 или v2)

3. **Текущее состояние**
   - Существование директории `/usr/local/projects/ai_report`
   - Содержимое директории
   - Запущенные контейнеры (Qdrant, Streamlit)

4. **Порты**
   - Порт 8504 (Streamlit)
   - Порт 6333 (Qdrant)

5. **Процессы**
   - Запущенный Streamlit (через PID файл)
   - Процессы на портах

6. **Конфигурация**
   - Наличие config.env
   - Oracle настройки (без паролей)

7. **Ресурсы**
   - Дисковое пространство
   - Docker volumes

---

## 🚀 Быстрое развертывание

### Один скрипт (рекомендуется):

```bash
cd deploy
./safe_deploy.sh root@82.114.2.2
```

Скрипт выполнит все шаги автоматически с проверками безопасности.

---

## ⚠️ Важные моменты

### Перед развертыванием:

1. **Резервное копирование:**
   ```bash
   ssh root@82.114.2.2 "tar czf /tmp/ai_report_backup_$(date +%Y%m%d_%H%M%S).tar.gz /usr/local/projects/ai_report"
   ```

2. **Проверка старого Streamlit:**
   - Скрипт автоматически остановит процесс на порту 8504
   - Проверит занятость порта перед развертыванием

3. **Конфигурация:**
   - Убедитесь, что `config.env` на сервере содержит правильные Oracle настройки
   - Если config.env не существует, будет создан из примера

### После развертывания:

1. **Проверка статуса:**
   ```bash
   ssh root@82.114.2.2 "cd /usr/local/projects/ai_report && ./status_all.sh"
   ```

2. **Проверка логов:**
   ```bash
   ssh root@82.114.2.2 "cd /usr/local/projects/ai_report && docker-compose logs -f streamlit"
   ```

3. **Проверка в браузере:**
   - Откройте `http://82.114.2.2:8504`
   - Перейдите на закладку "🤖 Ассистент"
   - Проверьте работу поиска

---

## 🔄 Откат (Rollback)

Если что-то пошло не так:

```bash
ssh root@82.114.2.2
cd /usr/local/projects/ai_report

# Остановка новых контейнеров
docker-compose down

# Восстановление из бэкапа (если нужно)
tar xzf /tmp/ai_report_backup_YYYYMMDD_HHMMSS.tar.gz -C /

# Запуск старого Streamlit
./run_streamlit_background.sh
```

---

## 📊 Мониторинг после развертывания

### Проверка здоровья:

```bash
# Qdrant
ssh root@82.114.2.2 "curl http://localhost:6333/health"

# Streamlit
ssh root@82.114.2.2 "curl http://localhost:8504/_stcore/health"

# KB коллекция
ssh root@82.114.2.2 "curl http://localhost:6333/collections/kb_billing"
```

### Логи:

```bash
# Все логи
ssh root@82.114.2.2 "cd /usr/local/projects/ai_report && docker-compose logs -f"

# Только Streamlit
ssh root@82.114.2.2 "cd /usr/local/projects/ai_report && docker-compose logs -f streamlit"

# Только Qdrant
ssh root@82.114.2.2 "cd /usr/local/projects/ai_report && docker-compose logs -f qdrant"
```

---

## 🛠️ Troubleshooting

### Проблема: rsync не найден

```bash
# Установка rsync
sudo apt-get update
sudo apt-get install rsync
```

### Проблема: SSH доступ запрещен

```bash
# Проверка SSH ключей
ssh-keygen -t rsa -b 4096
ssh-copy-id root@82.114.2.2
```

### Проблема: Порт 8504 занят другим процессом

```bash
# Найти процесс
ssh root@82.114.2.2 "lsof -i :8504"

# Остановить вручную
ssh root@82.114.2.2 "kill -9 <PID>"
```

---

## 📚 Дополнительная документация

- Полная инструкция: `DEPLOYMENT_SERVER.md`
- Развертывание RAG: `DEPLOYMENT_RAG.md`
- Быстрый старт: `QUICK_DEPLOY.md`
- Резюме: `DEPLOYMENT_SUMMARY.md`

