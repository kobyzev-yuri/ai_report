# План развертывания на сервере 82.114.2.2

## 🎯 Цель

Безопасное развертывание RAG системы с Qdrant и Streamlit на production сервере с минимальным downtime.

## 📋 Пошаговый план

### Этап 1: Подготовка (локально)

**Время:** 5 минут

```bash
cd /mnt/ai/cnn/ai_report/deploy

# 1. Проверка предварительных требований
./check_prerequisites.sh

# 2. Проверка файлов
ls -la *.sh *.yml Dockerfile.streamlit
```

**Проверяет:**
- ✅ SSH и rsync установлены
- ✅ Все необходимые файлы на месте
- ✅ RAG модули доступны

---

### Этап 2: Обследование сервера

**Время:** 2-3 минуты

```bash
./server_inspection.sh root@82.114.2.2
```

**Что проверяется (безопасно, только чтение):**
- ✅ Docker и Docker Compose на сервере
- ✅ Текущее состояние `/usr/local/projects/ai_report`
- ✅ Запущенные контейнеры и процессы
- ✅ Занятые порты (8504, 6333)
- ✅ Конфигурация (config.env)
- ✅ Дисковое пространство

**Важно:** Этот этап не изменяет ничего на сервере!

**Ожидаемый результат:**
- Отчет о текущем состоянии
- Список найденных процессов/контейнеров
- Рекомендации по дальнейшим действиям

---

### Этап 3: Резервное копирование (рекомендуется)

**Время:** 2-5 минут (зависит от размера)

```bash
ssh root@82.114.2.2 "tar czf /tmp/ai_report_backup_$(date +%Y%m%d_%H%M%S).tar.gz /usr/local/projects/ai_report"
```

**Создает:**
- Полный бэкап текущего состояния
- Расположение: `/tmp/ai_report_backup_YYYYMMDD_HHMMSS.tar.gz`

---

### Этап 4: Синхронизация файлов

**Время:** 1-3 минуты

```bash
./sync_deploy.sh root@82.114.2.2
```

**Синхронизирует:**
- ✅ Все файлы из deploy директории
- ✅ RAG модули (`kb_billing/rag/`)
- ✅ База знаний (KB данные)
- ✅ Docker файлы (docker-compose.yml, Dockerfile)
- ✅ Скрипты управления

**Требования:**
- rsync должен быть установлен локально
- SSH доступ к серверу

---

### Этап 5: Безопасное развертывание

**Время:** 5-10 минут

```bash
./safe_deploy.sh root@82.114.2.2
```

**Выполняет автоматически:**
1. ✅ Обследование сервера
2. ✅ Синхронизацию файлов
3. ✅ Остановку старого Streamlit на порту 8504
4. ✅ Проверку конфигурации
5. ✅ Развертывание через Docker Compose
6. ✅ Проверку развертывания

**Интерактивные шаги:**
- Подтверждение продолжения после обследования
- Выбор метода развертывания (Docker Compose / Ручное)

---

### Этап 6: Проверка развертывания

**Время:** 2-3 минуты

```bash
# Статус всех сервисов
ssh root@82.114.2.2 "cd /usr/local/projects/ai_report && ./status_all.sh"

# Проверка здоровья
ssh root@82.114.2.2 "curl http://localhost:6333/health"
ssh root@82.114.2.2 "curl http://localhost:8504/_stcore/health"

# Проверка KB
ssh root@82.114.2.2 "curl http://localhost:6333/collections/kb_billing | jq '.result.points_count'"
```

**Проверяет:**
- ✅ Qdrant доступен
- ✅ Streamlit доступен
- ✅ KB инициализирована
- ✅ Контейнеры запущены

---

### Этап 7: Тестирование

**Время:** 3-5 минут

1. **Откройте веб-интерфейс:**
   ```
   http://82.114.2.2:8504
   ```

2. **Проверьте закладку "🤖 Ассистент":**
   - Введите вопрос: "Покажи превышение трафика за октябрь 2025"
   - Нажмите "📊 Сгенерировать SQL"
   - Проверьте результаты

3. **Проверьте выполнение SQL:**
   - Выберите пример из результатов
   - Нажмите "▶️ Выполнить SQL"
   - Проверьте результаты запроса

---

## 🔄 Альтернативный план (ручное развертывание)

Если автоматическое развертывание не подходит:

### На сервере:

```bash
ssh root@82.114.2.2
cd /usr/local/projects/ai_report

# 1. Остановка старого Streamlit
if [ -f streamlit_8504.pid ]; then
    kill $(cat streamlit_8504.pid)
    rm streamlit_8504.pid
fi

# 2. Проверка конфигурации
cat config.env | grep -E '^ORACLE_|^QDRANT_'

# 3. Развертывание
./deploy.sh docker

# 4. Проверка
./status_all.sh
```

---

## ⚠️ Критические моменты

### Перед развертыванием:

1. **Резервное копирование** - обязательно!
2. **Проверка config.env** - Oracle настройки должны быть корректными
3. **Остановка старого Streamlit** - автоматически выполняется скриптом
4. **Проверка портов** - убедитесь, что 8504 и 6333 свободны

### После развертывания:

1. **Проверка логов** - убедитесь, что нет ошибок
2. **Тестирование** - проверьте работу ассистента
3. **Мониторинг** - следите за использованием ресурсов

---

## 🛡️ Безопасность

### Проверки безопасности:

1. ✅ Обследование не изменяет сервер (только чтение)
2. ✅ Остановка старого сервиса с проверкой
3. ✅ Валидация конфигурации перед развертыванием
4. ✅ Проверка конфликтов портов
5. ✅ Резервное копирование перед изменениями

### Рекомендации:

- Используйте SSH ключи вместо паролей
- Ограничьте доступ к портам через firewall
- Регулярно обновляйте Docker образы
- Мониторьте логи на наличие ошибок

---

## 📊 Ожидаемое время развертывания

- **Подготовка:** 5 минут
- **Обследование:** 2-3 минуты
- **Резервное копирование:** 2-5 минут
- **Синхронизация:** 1-3 минуты
- **Развертывание:** 5-10 минут
- **Проверка:** 2-3 минуты
- **Тестирование:** 3-5 минут

**Итого:** ~20-30 минут

---

## 🔄 Откат (если что-то пошло не так)

```bash
ssh root@82.114.2.2
cd /usr/local/projects/ai_report

# Остановка новых контейнеров
docker-compose down

# Восстановление из бэкапа
tar xzf /tmp/ai_report_backup_YYYYMMDD_HHMMSS.tar.gz -C /

# Запуск старого Streamlit
cd /usr/local/projects/ai_report
./run_streamlit_background.sh
```

---

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи: `docker-compose logs -f`
2. Проверьте статус: `./status_all.sh`
3. См. документацию: `DEPLOYMENT_SERVER.md`
4. Troubleshooting: `DEPLOYMENT_RAG.md`

---

## ✅ Чеклист развертывания

- [ ] Проверка предварительных требований (`check_prerequisites.sh`)
- [ ] Обследование сервера (`server_inspection.sh`)
- [ ] Резервное копирование
- [ ] Синхронизация файлов (`sync_deploy.sh`)
- [ ] Безопасное развертывание (`safe_deploy.sh`)
- [ ] Проверка статуса (`status_all.sh`)
- [ ] Проверка здоровья сервисов
- [ ] Тестирование веб-интерфейса
- [ ] Проверка работы ассистента
- [ ] Мониторинг логов

---

## 📚 Документация

- Быстрый старт: `QUICK_DEPLOY.md`
- Развертывание на сервере: `DEPLOYMENT_SERVER.md`
- Развертывание RAG: `DEPLOYMENT_RAG.md`
- Резюме: `DEPLOYMENT_SUMMARY.md`



