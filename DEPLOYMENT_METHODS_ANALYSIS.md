# Анализ методов развертывания проекта ai_report

## 📋 Обзор проекта

**ai_report** - это система интеллектуального интерфейса к финансовым отчетам по услугам Iridium M2M с:
- RAG-ассистентом на базе GPT-4o для генерации SQL запросов
- Векторной базой данных Qdrant для базы знаний
- Веб-интерфейсом на Streamlit
- Oracle Database для хранения данных
- База знаний (KB) по биллингу для обучения AI-агента

**Изначально разработан в:** Cursor IDE  
**Боевой сервер:** 82.114.2.2 (порт SSH: 1194)  
**Директория на сервере:** `/usr/local/projects/ai_report`

---

## 🚀 Методы развертывания на боевой сервер

### Метод 1: Автоматическое безопасное развертывание (Рекомендуется)

**Скрипт:** `deploy/safe_deploy.sh`

**Описание:** Полностью автоматизированное развертывание с проверками безопасности на каждом этапе.

**Использование:**
```bash
cd deploy
SSH_CMD="ssh -p 1194" ./safe_deploy.sh root@82.114.2.2
```

**Что выполняется:**
1. ✅ **Обследование сервера** - проверка текущего состояния
2. ✅ **Синхронизация файлов** - rsync всех необходимых файлов
3. ✅ **Остановка старого Streamlit** - безопасная остановка процесса на порту 8504
4. ✅ **Проверка конфигурации** - валидация config.env
5. ✅ **Развертывание контейнеров** - Docker Compose или ручной режим
6. ✅ **Проверка развертывания** - health checks для Qdrant и Streamlit

**Преимущества:**
- Полностью автоматизирован
- Безопасные проверки на каждом шаге
- Интерактивные подтверждения
- Автоматический откат при ошибках

**Недостатки:**
- Требует интерактивного ввода

---

### Метод 2: Docker Compose развертывание

**Скрипт:** `deploy/deploy.sh docker`

**Описание:** Контейнеризованное развертывание через Docker Compose.

**Использование:**
```bash
cd deploy

# Настройка конфигурации
cp config.env.example config.env
nano config.env  # Заполнить Oracle настройки

# Развертывание
./deploy.sh docker
```

**Что включает:**
- **Qdrant** - векторная БД (порт 6333)
- **Streamlit** - веб-интерфейс (порт 8504)
- **Автоматическая инициализация KB** при первом запуске

**Преимущества:**
- ✅ Изоляция окружения
- ✅ Автоматическая оркестрация сервисов
- ✅ Health checks и автоматический перезапуск
- ✅ Простое управление (docker-compose up/down)

**Недостатки:**
- Требует Docker и Docker Compose на сервере

**Управление:**
```bash
# Статус
docker-compose ps
./status_all.sh

# Логи
docker-compose logs -f streamlit
docker-compose logs -f qdrant

# Остановка
docker-compose down

# Перезапуск
docker-compose restart streamlit
```

---

### Метод 3: Ручное развертывание

**Скрипт:** `deploy/deploy.sh manual`

**Описание:** Развертывание без Docker Compose (Qdrant через Docker, Streamlit напрямую).

**Использование:**
```bash
cd deploy

# 1. Настройка
cp config.env.example config.env
nano config.env

# 2. Установка зависимостей
pip install -r requirements.txt

# 3. Запуск Qdrant
docker run -d \
  --name ai_report_qdrant \
  --restart unless-stopped \
  -p 6333:6333 \
  -p 6334:6334 \
  -v qdrant_storage:/qdrant/storage \
  qdrant/qdrant:latest

# 4. Инициализация KB
./init_kb.sh

# 5. Запуск Streamlit
./run_streamlit_background.sh
```

**Преимущества:**
- ✅ Больше контроля над процессом
- ✅ Проще отладка
- ✅ Не требует Docker Compose

**Недостатки:**
- Больше ручных шагов
- Нужно управлять процессами отдельно

---

### Метод 4: Синхронизация через rsync

**Скрипт:** `deploy/sync_deploy.sh`

**Описание:** Только синхронизация файлов без развертывания.

**Использование:**
```bash
cd deploy
SSH_CMD="ssh -p 1194" ./sync_deploy.sh root@82.114.2.2
```

**Что синхронизируется:**
- ✅ Основные файлы приложения (Streamlit)
- ✅ RAG модули (`kb_billing/rag/`)
- ✅ База знаний (KB данные)
- ✅ Oracle скрипты (tables, views, functions, data)
- ✅ Python скрипты
- ✅ Docker файлы
- ✅ Скрипты управления
- ✅ Документация

**Преимущества:**
- Быстрая синхронизация изменений
- Не затрагивает запущенные процессы
- Можно применить изменения вручную

**Недостатки:**
- Не выполняет развертывание
- Требует ручного применения изменений

---

### Метод 5: Обновление базы знаний (KB)

**Скрипты:**
- `sync_and_update_kb.sh` - синхронизация + обновление KB (без пересоздания)
- `sync_and_rebuild_kb.sh` - синхронизация + полная перестройка KB

**Использование:**

**Вариант A: Обновление KB (добавление новых данных)**
```bash
SSH_CMD="ssh -p 1194" ./sync_and_update_kb.sh root@82.114.2.2
```

**Вариант B: Полная перестройка KB (удаление старых данных)**
```bash
SSH_CMD="ssh -p 1194" ./sync_and_rebuild_kb.sh root@82.114.2.2
```

**Что синхронизируется:**
- RAG модули (`kb_billing/rag/*.py`)
- Описания таблиц (`kb_billing/tables/*.json`)
- Описания представлений (`kb_billing/views/*.json`)
- Примеры SQL запросов (`kb_billing/training_data/*.json`)

**Преимущества:**
- Быстрое обновление базы знаний
- Автоматическая загрузка в Qdrant
- Не требует перезапуска Streamlit

---

### Метод 6: Минимальный деплой (подготовка пакета)

**Скрипт:** `prepare_deployment.sh`

**Описание:** Создание минимального пакета для развертывания.

**Использование:**
```bash
./prepare_deployment.sh

# Создается директория deploy/ с минимальным набором файлов
tar -czf ai_report_deploy.tar.gz deploy/

# Перенос на сервер
scp -P 1194 ai_report_deploy.tar.gz root@82.114.2.2:/tmp/

# На сервере
ssh -p 1194 root@82.114.2.2
cd /usr/local/projects
tar -xzf /tmp/ai_report_deploy.tar.gz
mv deploy ai_report
cd ai_report
./deploy.sh docker
```

**Что включено:**
- ✅ Oracle скрипты
- ✅ Python приложение
- ✅ База знаний (KB)
- ✅ Скрипты управления
- ✅ Минимальная документация

**Что НЕ включено:**
- ❌ Архивы (archive/)
- ❌ Тесты (tests/)
- ❌ Временные файлы
- ❌ Данные (загружаются через интерфейс)

**Размер:** ~2-5 MB (без данных)

---

## 🔧 Вспомогательные инструменты

### Обследование сервера

**Скрипт:** `deploy/server_inspection.sh`

```bash
SSH_CMD="ssh -p 1194" ./server_inspection.sh root@82.114.2.2
```

**Проверяет:**
- Подключение к серверу
- Docker и Docker Compose
- Текущее состояние директории
- Запущенные контейнеры
- Занятые порты (8504, 6333)
- Запущенные процессы Streamlit
- Дисковое пространство
- Конфигурацию (config.env)

### Быстрая проверка

**Скрипт:** `deploy/quick_inspect.sh`

```bash
./quick_inspect.sh root@82.114.2.2
```

Автоматически определяет SSH команду и выполняет быстрое обследование.

### Проверка предварительных требований

**Скрипт:** `deploy/check_prerequisites.sh`

```bash
./check_prerequisites.sh
```

Проверяет локальные требования (SSH, rsync, файлы).

---

## 📊 Сравнение методов

| Метод | Автоматизация | Безопасность | Сложность | Время | Рекомендация |
|-------|---------------|--------------|-----------|-------|--------------|
| safe_deploy.sh | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐ | ~5 мин | ✅ Первое развертывание |
| Docker Compose | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ | ~3 мин | ✅ Production |
| Ручное | ⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ | ~10 мин | Отладка |
| rsync | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐ | ~1 мин | Обновления |
| KB Update | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐ | ~2 мин | ✅ Обновление KB |
| Минимальный пакет | ⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | ~15 мин | Новый сервер |

---

## 🎯 Рекомендуемый workflow

### Первое развертывание
```bash
cd deploy
SSH_CMD="ssh -p 1194" ./safe_deploy.sh root@82.114.2.2
```

### Обновление кода
```bash
cd deploy
SSH_CMD="ssh -p 1194" ./sync_deploy.sh root@82.114.2.2
ssh -p 1194 root@82.114.2.2 "cd /usr/local/projects/ai_report && docker-compose restart streamlit"
```

### Обновление базы знаний
```bash
SSH_CMD="ssh -p 1194" ./sync_and_update_kb.sh root@82.114.2.2
```

### Обновление Oracle VIEW
```bash
# 1. Синхронизация
cd deploy
SSH_CMD="ssh -p 1194" ./sync_deploy.sh root@82.114.2.2

# 2. Применение VIEW
ssh -p 1194 root@82.114.2.2 "source /usr/local/projects/ai_report/config.env && \
  cd /usr/local/projects/ai_report/oracle/views && \
  sqlplus \${ORACLE_USER}/\${ORACLE_PASSWORD}@\${ORACLE_HOST}:\${ORACLE_PORT}/\${ORACLE_SERVICE} \
  @04_v_consolidated_report_with_billing.sql"
```

---

## 🔐 Безопасность

### Рекомендации:

1. **Резервное копирование перед развертыванием:**
   ```bash
   ssh -p 1194 root@82.114.2.2 "tar czf /tmp/ai_report_backup_$(date +%Y%m%d_%H%M%S).tar.gz /usr/local/projects/ai_report"
   ```

2. **Защита config.env:**
   ```bash
   chmod 600 config.env
   ```

3. **Не экспонировать Qdrant наружу:**
   - Использовать только внутри Docker сети
   - Или ограничить доступ через firewall

4. **Использовать reverse proxy (nginx):**
   - Для Streamlit через BASE_URL_PATH
   - SSL/TLS для HTTPS

5. **Регулярные бэкапы Qdrant:**
   ```bash
   docker exec ai_report_qdrant tar czf /qdrant/storage/backup.tar.gz /qdrant/storage
   ```

---

## 🐛 Troubleshooting

### Порт 8504 занят
```bash
# Найти процесс
ssh -p 1194 root@82.114.2.2 "lsof -i :8504"

# Остановить
ssh -p 1194 root@82.114.2.2 "kill -9 <PID>"
```

### Qdrant не запускается
```bash
# Проверка логов
ssh -p 1194 root@82.114.2.2 "docker logs ai_report_qdrant"

# Перезапуск
ssh -p 1194 root@82.114.2.2 "docker restart ai_report_qdrant"
```

### KB не инициализирована
```bash
# Проверка коллекции
ssh -p 1194 root@82.114.2.2 "curl http://localhost:6333/collections/kb_billing"

# Переинициализация
ssh -p 1194 root@82.114.2.2 "cd /usr/local/projects/ai_report && ./init_kb.sh --recreate"
```

### Streamlit не запускается
```bash
# Проверка логов
ssh -p 1194 root@82.114.2.2 "cd /usr/local/projects/ai_report && docker-compose logs streamlit"

# Перезапуск
ssh -p 1194 root@82.114.2.2 "cd /usr/local/projects/ai_report && docker-compose restart streamlit"
```

---

## 📁 Структура файлов развертывания

```
ai_report/
├── deploy/                          # Директория развертывания
│   ├── safe_deploy.sh              # ⭐ Безопасное автоматическое развертывание
│   ├── deploy.sh                   # Развертывание (docker/manual)
│   ├── sync_deploy.sh              # Синхронизация файлов
│   ├── server_inspection.sh        # Обследование сервера
│   ├── quick_inspect.sh            # Быстрая проверка
│   ├── check_prerequisites.sh      # Проверка требований
│   ├── init_kb.sh                  # Инициализация KB
│   ├── update_kb.sh                # Обновление KB
│   ├── status_all.sh               # Статус всех сервисов
│   ├── stop_all.sh                 # Остановка всех сервисов
│   ├── docker-compose.yml          # Docker Compose конфигурация
│   ├── Dockerfile.streamlit        # Dockerfile для Streamlit
│   ├── config.env.example          # Пример конфигурации
│   └── DEPLOYMENT_*.md             # Документация
│
├── sync_and_update_kb.sh           # Синхронизация + обновление KB
├── sync_and_rebuild_kb.sh          # Синхронизация + перестройка KB
├── prepare_deployment.sh           # Подготовка минимального пакета
├── .rsyncignore                    # Исключения для rsync
│
├── run_streamlit_background.sh     # Запуск Streamlit в фоне
├── stop_streamlit.sh               # Остановка Streamlit
├── status_streamlit.sh             # Статус Streamlit
├── restart_streamlit.sh            # Перезапуск Streamlit
│
└── cursor.md                       # ⭐ Инструкция для Cursor IDE
```

---

## 📖 Документация

### Основная документация:
- **README.md** - Общее описание проекта
- **cursor.md** - Инструкция по работе с проектом в Cursor IDE
- **DEPLOY_CHECKLIST.md** - Чеклист деплоя

### Документация развертывания (deploy/):
- **START_HERE.md** - ⭐ Начните отсюда
- **DEPLOYMENT_PLAN.md** - План развертывания
- **DEPLOYMENT_SERVER.md** - Развертывание на сервере
- **DEPLOYMENT_RAG.md** - Развертывание RAG системы
- **DEPLOYMENT_SUMMARY.md** - Резюме развертывания
- **QUICK_DEPLOY.md** - Быстрый старт

---

## 🌐 Доступ к приложению

После развертывания приложение доступно по адресу:

**Прямой доступ:** `http://82.114.2.2:8504`  
**Через Nginx proxy:** `http://stat.steccom.ru:7776/ai_report`

**Основные вкладки:**
- 🤖 **Ассистент** - RAG-ассистент для генерации SQL запросов
- 📈 **Финансовый анализ** - анализ прибыльности и тенденций
- **Главный отчет** - сводная таблица по IMEI
- **Data Loader** - загрузка CSV/Excel файлов

---

## 🎓 Ключевые особенности проекта

1. **Разработан в Cursor IDE** - использует AI-ассистент для разработки
2. **RAG-система** - векторный поиск по базе знаний для генерации SQL
3. **Модульная архитектура** - легко обновлять отдельные компоненты
4. **Автоматизированный деплой** - множество скриптов для разных сценариев
5. **Безопасность** - проверки на каждом этапе развертывания
6. **Документация** - подробная документация для всех процессов

---

## ✅ Чеклист после развертывания

- [ ] Qdrant доступен на порту 6333
- [ ] Streamlit доступен на порту 8504
- [ ] KB инициализирована (проверить коллекцию)
- [ ] Ассистент работает (открыть закладку "🤖 Ассистент")
- [ ] Поиск примеров работает
- [ ] Выполнение SQL запросов работает
- [ ] Логи не содержат ошибок
- [ ] Веб-интерфейс доступен через nginx proxy

### Команды проверки:
```bash
# Статус всех сервисов
ssh -p 1194 root@82.114.2.2 "cd /usr/local/projects/ai_report && ./status_all.sh"

# Проверка здоровья
ssh -p 1194 root@82.114.2.2 "curl http://localhost:6333/health"
ssh -p 1194 root@82.114.2.2 "curl http://localhost:8504/_stcore/health"

# Проверка KB
ssh -p 1194 root@82.114.2.2 "curl http://localhost:6333/collections/kb_billing | jq '.result.points_count'"
```

---

## 📞 Дополнительная информация

**Проект:** Iridium M2M Reporting  
**Дата создания:** Октябрь 2025  
**Технологии:** Oracle 11g+, Python 3.10+, Streamlit, Qdrant, Docker

**Контакты и поддержка:** См. README.md в корне проекта
